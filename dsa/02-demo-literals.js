const name = "Alice"
const age = 28
const points = 1500

// Simple Literals
const str1 = "Hello World"
const str2 = 'Hello World'
const str3 = `Hello World`

// Multiline Strings
const multiline = `This is a string
that spans multiple
lines.`
// Interpolation
const message = `Hello, my name is ${name}. I am ${age} years old and have ${points} points.`;

// Concatenation
const conc = "Hello, my name is " + name + ". I am " + age + " years old and have " + points + " points.";
console.log(message);
console.log(conc);